DATABASE_URL=postgres://user:pass@ep-xxxx.neon.tech/neondb?sslmode=require
SESSION_SECRET=replace-with-a-long-random-string
# Optional: required only for AI features (story generation / translation)
# OPENAI_API_KEY=sk-...
# Optional: run production locally over HTTP
# SESSION_COOKIE_SECURE=false
# Optional: pick a port if 5000 is in use
# PORT=3000
